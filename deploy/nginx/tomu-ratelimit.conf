# Per-IP throttle for Tomu auth endpoints.
# Include from nginx http{} context — e.g. install to /etc/nginx/conf.d/tomu-ratelimit.conf.
limit_req_zone $binary_remote_addr zone=tomu_auth:10m rate=5r/m;
